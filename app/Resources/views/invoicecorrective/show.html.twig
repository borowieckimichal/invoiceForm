{% extends 'base.html.twig' %}

{% block body %}
    <h1>Invoicecorrective</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoiceCorrective.id }}</td>
            </tr>
            <tr>
                <th>Invoicecorrected</th>
                <td>{{ invoiceCorrective.invoiceCorrected }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ invoiceCorrective.comments }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('invoicecorrective_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('invoicecorrective_edit', { 'id': invoiceCorrective.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
